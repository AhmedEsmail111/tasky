class Task {
  Task({
    required this.id,
    required this.title,
    required this.date,
    required this.time,
    required this.status,
  });
  final int id;
  final String title;
  final String date;
  final String time;
  final String status;
}


// play a sound when the user clicks the done button 
// create a progress indicator while loading
// delete archived tasks and add notes page
